// Your code here:

const numBottles = [99]
let noBottle = "No more bottles"
let final = "Go to the store and buy some more, "

for (let i = 100; i < 1; i--) {

    numBottles[i] = numInEstrofa
    
    if (numBottles == 0) {

        numInEstrofa += 

    }

    let estrofa = `${} bottles of milk on the wall, ${} bottles of milk. Take one down and pass it around, `

}